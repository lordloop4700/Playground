#include <string>
#include <vector>
#include <algorithm>
//[93,30,55]	[1,30,5]	[2,1]
using namespace std;

vector<int> solution(vector<int> progresses, vector<int> speeds) {
	vector<int> answer;
	vector<int> check;

	for (int i = 0; i < progresses.size(); i++) {//계속해서 더하는 루틴
		for (int j = 0; j < progresses.size(); i++) {

		}
	}



	return answer;


}
	// 반복문이 돌면서 체크하다가 100이 넘으면 같은 i번째 다른 배열에다가 같은 위치에 넣어 버릴까?

	// 100이 될 때 해당 위치를 정렬된 vector와 비교하여...

	//100이 될 때 마다 앞이 100인지 확인 (아니면 반대로 100이 될 때마다 뒤가 100이 아닌지 확인)
	
